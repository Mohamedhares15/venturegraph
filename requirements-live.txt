# VentureGraph Live Engine — minimal Linux runner dependencies
# Used by .github/workflows/live_engine.yml
# Excludes heavy/incompatible packages: xhtml2pdf, pyvis, streamlit, statsmodels,
# python-louvain, matplotlib, seaborn (all only needed for offline analysis).

pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
beautifulsoup4>=4.12.0

# Supabase + ingest
supabase>=2.10.0
feedparser>=6.0.11
python-dotenv>=1.0.0

# Market data
yfinance>=0.2.55
